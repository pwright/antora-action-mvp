name: A workflow for my Hello World file
on: push
jobs:
  build:
    runs-on: ubuntu-latest    
    steps:
      - name: checkout
        uses: actions/checkout@v1
      - name: run script
        uses: ./action-a
        env:
          MY_NAME: "Red Hat"

        